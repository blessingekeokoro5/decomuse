Collection hero images for DecoMuse room pages.

Save each room's hero photo here with these exact names:

  bathroom.jpg   → used by bathroom.html

The image shows as a full-width banner behind the black "Collection · Bathroom"
box, so a wide landscape photo (approx 1800 x 900px) looks best. A portrait photo
also works — it will be centre-cropped to fill the banner.

Until the file is added, the banner shows a soft cream fallback background.
