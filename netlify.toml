# ============================================================
#  DecoMuse — Netlify configuration
#  Static site + serverless Stripe checkout.
# ============================================================

[build]
  publish = "."
  functions = "netlify/functions"
  # Rebuild the trusted server-side price catalogue from js/data.js on every
  # deploy, so checkout prices can never drift from the shop.
  command = "node scripts/gen-catalogue.js"

[functions]
  node_bundler = "esbuild"

# Friendly alias: POST /api/create-checkout-session
# -> /.netlify/functions/create-checkout-session
[[redirects]]
  from = "/api/create-checkout-session"
  to = "/.netlify/functions/create-checkout-session"
  status = 200
  force = true

[[redirects]]
  from = "/api/issue-gift-card"
  to = "/.netlify/functions/issue-gift-card"
  status = 200
  force = true

[[redirects]]
  from = "/api/stripe-webhook"
  to = "/.netlify/functions/stripe-webhook"
  status = 200
  force = true

# Sensible security headers for a retail site
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "SAMEORIGIN"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "geolocation=(), microphone=(), camera=()"
